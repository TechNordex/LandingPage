const fetch = require('node-fetch');

async function main() {
    try {
        // Find a session token from db, or just spoof the cookie if it's iron-session
        // Actually, we can just make the db script fetch it without auth by mocking session 
        // Or I can just check the node server logs
    } catch(e) {}
}
